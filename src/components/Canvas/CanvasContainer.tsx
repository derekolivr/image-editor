import { ImageCanvas } from "./ImageCanvas";

export const CanvasContainer = () => {
  return (
    <div className="flex-grow flex items-center justify-center bg-card p-4">
      <ImageCanvas />
    </div>
  );
};
